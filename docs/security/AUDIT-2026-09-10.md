# Auditoria jamproject-octopus — 2026-09-10

**Escopo:** 20 arquivos. App desktop Python/Qt local (`octopus.py`, PyQt `QDialog`/`QMenu`/`QApplication`),
sem servidor, sem HTML, sem Docker. CI em `.github/workflows/ci.yml` e `release.yml`.

**Não coberto:** não há rota HTTP, template HTML nem infra de deploy — checklist de rede/WCAG não se
aplica. Não abri `docs/preview.svg`.

## P0
Nenhum.

## P1
Nenhum.

## P2
- **Onde:** `.github/workflows/ci.yml:14,16,46,48` e `.github/workflows/release.yml:37,39,81,93,103`
  **Problema:** todas as `uses:` (`actions/checkout@v4`, `actions/setup-python@v5`,
  `actions/upload-artifact@v4`, `actions/download-artifact@v4`, `softprops/action-gh-release@v2`)
  referenciam tag mutável, não SHA de 40 chars.
  **Impacto:** se a tag for reescrita (typosquat/compromisso da action), o workflow roda código não
  auditado sem o repo mudar nada.
  **Correção:** pinar cada `uses:` no SHA do commit correspondente à versão usada.

## P3
Nenhum.

## Verificado e OK
- Nenhum `pull_request_target` nos workflows; nenhum `secrets.*` referenciado (nada a vazar em log).
- `octopus.py`: os únicos `.exec(` encontrados são `QMenu.exec()`/`QDialog.exec()`/`QApplication.exec()`
  (loop de evento Qt), não `eval`/`exec` de Python sobre string — sem injeção de código.
- Nenhum segredo (`sk-ant-`, `ghp_`, `AKIA`, `-----BEGIN`, `api_key=`) nem `.env`/`.pem`/`.pfx`/`.p12`/`.key`
  versionado.
